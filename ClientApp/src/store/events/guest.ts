export interface Guest {
  /** The full name of the guest. */
  name: string;

  /** The name of the company the guest is working for. */
  company: string;

  /** The job title of the guest at the company. */
  title: string;
}
